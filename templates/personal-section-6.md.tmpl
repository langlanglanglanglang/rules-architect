## 六、规则架构维护（人工流程，不自动判断）

记忆系统分为五层：**L0 Hook** / **L1 Memory** / **L2 路径规则** /
**L3 CLAUDE.md** / **L5 团队 Lessons**。本节规定 L1 与 L3/L5 之间的升级、
退役和同步流程，全部由人工触发；平台没有可靠的 `trigger_count` 持久化机制，
因此不做自动判断。

### 6.5 五问归位 SOP（强制）

出现任何新规则时（用户纠正、用户说“以后……”、发现重复模式），保存前必须
完成以下五问：

| # | 问题 | 选择层级 |
|---|---|---|
| 1 | 触发条件能用 tool/matcher 精确表达吗？ | **L0 Hook** / **L2 路径规则** |
| 2 | 只在编辑特定文件或目录时相关吗？ | **L2** `.claude/rules/` |
| 3 | 全团队都需要吗（包括 Codex/Gemini 用户）？ | **L3** CLAUDE.md / **L5** Lessons |
| 4 | 只是个人协作偏好吗？ | **L1 Memory** |
| 5 | 与已有规则重叠吗？先检索；重叠则修改已有规则 | — |

**禁止**：跳过五问，直接把规则放入 L1 Memory。

示例：

- “编辑 `.proto` 时字段编号递增” → L2 `.claude/rules/proto.md`
- “经常忘记附 MR 链接” → L0 PostToolUse Hook
- “我偏好简洁报告” → L1 Memory
- “团队发布规则” → L3 CLAUDE-company.md

三层归位提醒：

- `memory_intake_check.py`：写入 Memory 前注入五问。
- `rule_intake_reminder.py`：用户提出新规则时注入五问。
- `.claude/rules/rule-intake.md`：编辑规则文件时注入五问。

L0 Hook 与 L2 路径规则更接近相关动作或文件，但 `additionalContext` Hook
仍是提醒，不是阻断控制。本节用于人工阅读和团队对齐。

### 升级（Memory → CLAUDE-personal.md / CLAUDE-company.md）

触发：用户要求检查 Memory、升级某条反馈，或把反复遗忘的规则写入 CLAUDE.md。

步骤：

1. 阅读对应的 `~/.claude/projects/.../memory/feedback_*.md`。
2. 提取规则、原因和应用方式。
3. 将正文追加到 `CLAUDE-personal.md` 或 `CLAUDE-company.md` 的对应章节。
4. 将 Memory 正文替换为 `Promoted to: <path>:<section> @ YYYY-MM-DD`。
5. 保留 MEMORY.md 索引项。

### 退役（Memory 软删除）

触发：用户确认规则已过时、与现状冲突或引用对象已不存在。

步骤：

1. 检索当前代码和 Git 历史，确认规则确实失效。
2. 在 Memory 正文末尾追加 `Deprecated: YYYY-MM-DD — <原因>`。
3. 从 MEMORY.md 索引删除对应行，保留文件用于历史追踪。

### 团队同步（Memory → lessons.md）

触发：用户要求把某条反馈同步到团队 Lessons。

```bash
# 预览
python3 ~/.claude/skills/rules-architect/scripts/memory_sync.py push \
  --feedback feedback_xxx --dry-run

# 写入
python3 ~/.claude/skills/rules-architect/scripts/memory_sync.py push \
  --feedback feedback_xxx --reason "..."
```

只允许单向推送，不反向拉取：Memory 是单条经验，Lessons 是按主题聚合的多人文档，
两者结构不同。

### 不实现的功能

- 自动统计 `trigger_count`
- 依赖平台未保证保留的私有 frontmatter 字段
- 自动判断是否升级
- L5 → L1 反向同步

### Hook 健康状态

三个核心 Hook 位于 `~/.claude/hooks/`，命中记录写入
`{{CACHE_DIR}}/audit.jsonl`：

- `memory_intake_check.py`：写入 Memory 时注入五问。
- `rule_intake_reminder.py`：用户消息包含规则关键词时注入五问。
- `cleanup_hook.py`：会话启动时清理锁和轮转审计日志。

可选工作流 Hook 位于 `examples/`。

查看命中：

```bash
jq -c 'select(.decision == "inject") | {hook, ts}' {{CACHE_DIR}}/audit.jsonl | tail
```

Manifest：

```bash
cat ~/.claude/.rules-architect-manifest.json
```
